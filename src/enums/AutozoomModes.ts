/**
 * Lower case naming scheme to match target reported values.
 */
enum AutozoomModes {
  NORMAL = 'normal',
  PLAZA = 'plaza',
  PLAZA_DUPLICATE = 'plaza_duplicate',
}

export default AutozoomModes;
